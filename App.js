import React from 'react';
import {SafeAreaView, ScrollView, View, Text} from 'react-native';

const App = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <View>
          <Text>This is a demo for unit testing</Text>
        </View>
      </ScrollView>
    </SafeAreaView>
  );
};

export default App;
